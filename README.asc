= Python example files

This is a collection of very short (hopefully) python files that just give some examples of how to manipulate and plot data. 
It is meant as a supplement to the MatPlotLib example pages and Stack overflow. 

The files names should be reasonably self explanatory. 